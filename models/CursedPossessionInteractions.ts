export default class CursedPossessionInteractions {
    private type: string
    private interactions: string[]

    constructor(type: string, interactions: string[]) {
        this.type = type
        this.interactions = interactions
    }
}